import { Request, Response, RequestHandler } from 'express';

export default class DiscountController {
    getAllDiscounts: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    saveDiscount: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    updateDiscount: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    deleteDiscount: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    searchDiscount: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };
};