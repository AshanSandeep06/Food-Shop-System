import { Request, Response, RequestHandler } from 'express';

const DiscountController = () => {
    const getAllDiscounts: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    const saveDiscount: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    const updateDiscount: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    const deleteDiscount: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    const searchDiscount: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };
};

export default DiscountController;