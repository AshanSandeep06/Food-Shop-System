import { Request, Response, RequestHandler } from 'express';

const ReservationController = () => {
    const getAllReservations: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    const saveReservation: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    const updateReservation: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    const deleteReservation: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    const searchReservation: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };
};

export default ReservationController;