import { Request, Response, RequestHandler } from 'express';

export default class ReservationController {
    getAllReservations: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    saveReservation: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    updateReservation: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    deleteReservation: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };

    searchReservation: RequestHandler = async(req: Request, res: Response): Promise<Response> => {
        return res;
    };
};
