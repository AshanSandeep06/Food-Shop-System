import React from "react";
import "./Header.css";

const Header = () => {
  return (
    // <!--Header-->
    <header id="homeHeader" className="row fixed-top text-black">
    </header>
  );
};

export default Header;
